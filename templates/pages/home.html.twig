{% extends 'base.html.twig' %}

{% block title 'Agence immobiliere '%}

{% block body %}

    <div class="jumbotron text-center">
       
{# SLIDER #}

<div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img class="d-block w-100" src="https://images.pexels.com/photos/439227/pexels-photo-439227.jpeg?auto=compress&cs=tinysrgb&h=400&w=800" alt="First slide">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100" src="https://images.pexels.com/photos/276700/pexels-photo-276700.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=400&w=800" alt="Second slide">
    </div>
  </div>
</div>
      </div>

    <div class="container">

        <h2>Les derniers biens</h2>

            <div class="card-deck">
            {% for property in properties %}
                  <div class="card">
                    <img class="card-img-top" src="{{ property.image }}" alt="Card image cap">
                    <div class="card-body">
                        <a href="{{path ('property.show', {'id': property.id, 'slug': property.slug}) }}"> {{ property.title }} </a>
                
                        <p class="card-text">{{ property.city }}</p>
                    </div>
                    <div class="card-footer">
                      <div class="text-primary" style="font-weight: bold;font-size: 2rem;">{{ property.formatedPrice }}€</div>
                    </div>
                  </div>
            
          {% endfor %}
      </div>
    </div>


{% endblock %}