{% extends 'base.html.twig' %}

{% block title 'Agence immobiliere '%}

{% block body %}

    <div class="jumbotron text-center">

        <h1>Agence immobiliere</h1>
        <p>lorem ipsum dolor sit asset</p>    
    </div>

    <div class="container">

        <h2>Les derniers biens</h2>

        <div class="row flex"> 

            {% for property in properties %}
                <div class="col-3">
                    <div class="card">
                            <div class="card-body">

                                <h5 class="card-title">

                                    <a href="{{path ('property.show', {'id': property.id, 'slug': property.slug}) }}"> {{ property.title }} </a>
                            
                                </h5>

                                <p class="card-text">{{ property.city }}</p>
                                <div class="text-primary" style="font-weight: bold;font-size: 2rem;">{{ property.formatedPrice }}€></div>
                            </div>
                    </div>
                </div> 
            {% endfor %}
        </div>
            
    </div>


{% endblock %}